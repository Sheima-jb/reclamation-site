import React from 'react';

const Column = ({ title }) => {
  return (
    <div className="column">
      <h2>{title}</h2>
      {/* Vous pouvez ajouter ici des tâches ou d'autres éléments */}
    </div>
  );
};

export default Column;