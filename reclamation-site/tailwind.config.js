/** @type {import('tailwindcss').Config} */
module.exports = {
  content: ["./src/**/*.{js,jsx,ts,tsx}"], // Vérifie bien que ce chemin correspond à ton projet
  theme: {
    extend: {},
  },
  plugins: [], // Assure-toi que ce soit bien un tableau (même vide)
};
